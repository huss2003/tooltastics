<div class="flex flex-1 flex-col {Astro.props.class ?? ''}">
  <div
    class="ruler-canvas-wrap relative flex-1 min-h-[300px] lg:min-h-[400px] rounded-xl border border-surface-200 dark:border-surface-700 bg-white dark:bg-surface-900 overflow-auto"
    data-canvas-container
    role="img"
    aria-label="Online ruler tool — measure objects on your screen"
  >
    <canvas
      id="ruler-canvas"
      class="block"
      role="application"
      aria-roledescription="ruler"
      aria-label="Interactive ruler. Click or tap to place a measurement marker. Use arrow keys to adjust."
      tabindex="0"
    />
  </div>

  <div
    class="mt-3 flex items-center justify-center gap-2 rounded-lg bg-surface-100 dark:bg-surface-800 px-4 py-3 text-center text-sm text-surface-500 dark:text-surface-400 min-h-[44px]"
    data-readout
    aria-live="polite"
    aria-atomic="true"
  >
    Click on the ruler to measure
  </div>
</div>

<style>
  .ruler-canvas-wrap {
    scroll-behavior: smooth;
    -webkit-overflow-scrolling: touch;
  }
  .ruler-canvas-wrap canvas {
    min-width: 100%;
    min-height: 100%;
  }
  @media (pointer: coarse) {
    .ruler-canvas-wrap {
      touch-action: pan-x pan-y;
    }
  }
</style>

<script>
  import { initRulerTool } from './ruler-init'
  const tool = initRulerTool()
</script>
